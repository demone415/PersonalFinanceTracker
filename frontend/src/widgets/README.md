# widgets

Composite UI blocks that assemble features and entities into a self-contained
section of a page (FSD `widgets` layer), e.g. `dashboard-summary`,
`accruals-table`, `app-sidebar`.

Widgets may depend on `features`, `entities` and `shared`.
